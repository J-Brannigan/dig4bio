name: 01_baseline

inputs:
  level: processed
  train_folder: source_grid_fingerprint_linear
  train_file: source_datasets.csv

outputs:
  level: processed
  folder: source_grid_fingerprint_cubic
  file: source_datasets.csv

cv:
  strategy: k-fold
  fold_column: fold_idx

wavenumber_grid:
  start: 300
  end: 1800
  step: 1