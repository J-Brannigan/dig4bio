name: source_grid_fingerprint_cubic

input:
  level: interim
  dataset: source_devices

outputs:
  level: processed
  folder: source_grid_fingerprint_cubic
  file: source_datasets.csv

wavenumber_grid:
  start: 300
  end: 1800
  step: 1

interpolation:
  method: cubic
  extrapolate: false

combine:
  add_device_column: true